# Web App / PWA / APK Environment Configuration
# Copy to .env.local for dev:  cp .env.example .env.local
# For APK builds, copy to .env.production with your live API URL.

# Points to your API (local dev or deployed Railway/Render URL)
VITE_API_BASE_URL=http://localhost:4000/api
VITE_APP_NAME=Kolo App
VITE_ENABLE_ANALYTICS=false
